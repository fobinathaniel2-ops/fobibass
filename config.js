// ======================================
// config.js
// One place to point the frontend at your deployed backend.
// Update API_BASE after deploying the /backend folder (see backend/README.md).
// ======================================

// The Express app serves the frontend and API from the same origin.
export const API_BASE = "";
